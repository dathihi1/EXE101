[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vsl-mvp"
version = "0.1.0"
description = "Vietnamese Sign Language MVP recognition training pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
